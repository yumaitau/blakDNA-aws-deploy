apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: {{ include "blakdna.fullname" . }}-web }
spec:
  maxUnavailable: 1
  selector: { matchLabels: { app.kubernetes.io/name: {{ include "blakdna.name" . }}, app.kubernetes.io/component: web } }
